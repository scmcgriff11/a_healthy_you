<%= form_for(@preparation_tip) do |f| %>
  <% if @preparation_tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preparation_tip.errors.count, "error") %> prohibited this preparation_tip from being saved:</h2>

      <ul>
      <% @preparation_tip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
