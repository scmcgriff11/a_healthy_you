<%= form_for(@condition_food) do |f| %>
  <% if @condition_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@condition_food.errors.count, "error") %> prohibited this condition_food from being saved:</h2>

      <ul>
      <% @condition_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
